{
  "project_name": "AI Council — Advanced Multi-Model Orchestration Platform",
  "version": "6.0.0-phase6",
  "checkpoint": "ai-council6-phase6-FINAL.zip",
  "checkpoint_date": "2026-05-23",
  "current_phase": "PHASE 6 — Boot Self-Test + Persistent History + Prompt Chaining + Selector Config UI",

  "completed_tasks": [
    "PHASE 0–5: All previous phases (see phase5 checkpoint docs)",

    "PHASE 6: main.js — runBootSelfTest() wired: auto-called after load-panes + after open-provider-view",
    "PHASE 6: main.js — sessionStore.init(userData) called before createWindow()",
    "PHASE 6: main.js — providerConfig.init(userData) called before createWindow()",
    "PHASE 6: main.js — registerConfigIPC(ipcMain, getView) registered",
    "PHASE 6: main.js — on-demand panel open also triggers runBootSelfTest([aiId])",

    "PHASE 6: session-store.js — init(userDataPath): sets up history.jsonl",
    "PHASE 6: session-store.js — add(): appends slim entry to file (async, fire-and-forget)",
    "PHASE 6: session-store.js — loadFromFile(): loads last 20 entries from jsonl on boot",
    "PHASE 6: session-store.js — _maybeRotateFile(): keeps file ≤200 lines",
    "PHASE 6: session-store.js — chainDepth field tracked per entry",
    "PHASE 6: session-store.js — getSummaries() includes chainDepth",

    "PHASE 6: src/config/provider-config.js — NEW: loads/saves userData/provider-config.json",
    "PHASE 6: provider-config.js — PROVIDER_DEFAULTS: hardcoded baseline selectors for all 6 providers",
    "PHASE 6: provider-config.js — getSelector(id, key): returns override ?? default",
    "PHASE 6: provider-config.js — getProvider(id): merged default+override with _hasOverride flag",
    "PHASE 6: provider-config.js — getAllProviders(): full list for settings UI",
    "PHASE 6: provider-config.js — setSelectors(id, changes): saves overrides, null=clear",
    "PHASE 6: provider-config.js — resetProvider(id), resetAll()",

    "PHASE 6: src/ipc/config-ipc.js — NEW: config-get-all, config-get-provider, config-set-selectors",
    "PHASE 6: config-ipc.js — config-reset-provider, config-reset-all",
    "PHASE 6: config-ipc.js — config-test-selector: runs querySelector in live BrowserView, returns { found, count, tag, text, error }",

    "PHASE 6: preload.js — configGetAll(), configGetProvider(), configSetSelectors()",
    "PHASE 6: preload.js — configResetProvider(), configResetAll(), configTestSelector()",

    "PHASE 6: agent.js — _chainDepth state variable",
    "PHASE 6: agent.js — chainFromResult(result): populates task textarea, increments chain depth, switches to recursive mode",
    "PHASE 6: agent.js — resetChain(): clears chain depth, clears textarea",
    "PHASE 6: agent.js — updateChainDepthBar(): shows/hides chain depth indicator bar",
    "PHASE 6: agent.js — 'chain-depth-bar' element: shows current chain depth with reset button",
    "PHASE 6: agent.js — '⬆ Use as Input' chain button on every successful result card",
    "PHASE 6: agent.js — 'chain-depth-badge' in results header and history rows",
    "PHASE 6: agent.js — '⚙️' settings button in agent header",
    "PHASE 6: agent.js — toggleSettingsPanel() / renderSettingsPanel()",
    "PHASE 6: agent.js — Settings panel: per-provider selector table with Test/Save/Reset buttons",
    "PHASE 6: agent.js — Test: calls configTestSelector(), shows ✓/✗ + match count + tag + text preview",
    "PHASE 6: agent.js — Save: calls configSetSelectors(), re-renders panel with override badge",
    "PHASE 6: agent.js — Reset: calls configResetProvider(), reloads panel",
    "PHASE 6: agent.js — 'Reset All' button calls configResetAll()",
    "PHASE 6: agent.js — History rows show chain depth badge (⛓N)",
    "PHASE 6: agent.js — History replay restores chainDepth from entry",
    "PHASE 6: agent.js — chainDepth passed to orchestrate() opts (for session store)",
    "PHASE 6: agent.js — All Phase 5 features preserved",

    "PHASE 6: style.css — .chain-depth-bar, .chain-icon, .chain-reset-btn",
    "PHASE 6: style.css — .chain-depth-badge (results header + history)",
    "PHASE 6: style.css — .chain-btn ('Use as Input' button on result cards)",
    "PHASE 6: style.css — .result-card-actions wrapper",
    "PHASE 6: style.css — .agent-settings-btn, .settings-panel, .settings-header",
    "PHASE 6: style.css — .settings-provider, .settings-provider-header, .override-badge",
    "PHASE 6: style.css — .sel-table, .sel-row, .sel-label, .sel-input, .sel-actions",
    "PHASE 6: style.css — .sel-test-btn, .sel-test-result (.pass/.fail/.testing)",
    "PHASE 6: style.css — .sel-save-btn, .sel-reset-btn, .settings-reset-all-btn",
    "PHASE 6: style.css — .panel-open/.panel-closed status indicators",
    "PHASE 6: style.css — @keyframes slideIn animation"
  ],

  "modified_files_phase6": [
    "main.js                              — runBootSelfTest wire, sessionStore.init, providerConfig.init, registerConfigIPC",
    "preload.js                           — config-* IPC bridges added",
    "src/memory/session-store.js          — persistent JSONL: init, loadFromFile, _appendToFile, _maybeRotateFile, chainDepth",
    "src/config/provider-config.js        — NEW: persistent selector override config",
    "src/ipc/config-ipc.js               — NEW: config get/set/reset/test IPC handlers",
    "renderer/agent.js                   — chain button, chain depth bar, settings panel, selector editor",
    "renderer/style.css                  — Phase 6 styles appended"
  ],

  "ipc_channels": {
    "orchestrate"           : "R→M: start run",
    "orchestrate-cancel"    : "R→M: cancel",
    "orchestrate-progress"  : "M→R: all engine events",
    "orchestrate-result"    : "M→R: final ranked results",
    "partial-response"      : "M→R: live streaming text per provider",
    "provider-selftest"     : "M→R: boot self-test coverage events",
    "get-providers"         : "R→M: list registered providers",
    "list-open-views"       : "R→M: list provider IDs with open BrowserViews",
    "open-provider-view"    : "R→M: open a provider panel on demand",
    "views-loaded"          : "M→R: push after load-panes with openIds list",
    "health-get-all"        : "R→M: all health statuses",
    "health-check-now"      : "R→M: force health check",
    "health-status"         : "M→R: push on provider health change",
    "memory-get-history"    : "R→M: list session summaries",
    "memory-get-entry"      : "R→M: full entry by id",
    "memory-clear"          : "R→M: wipe session",
    "memory-remove"         : "R→M: delete one entry",
    "memory-size"           : "R→M: count entries",
    "export-formats"        : "R→M: list formats",
    "export-results"        : "R→M: save to disk via dialog",
    "config-get-all"        : "R→M: all provider selector configs (defaults+overrides)",
    "config-get-provider"   : "R→M: single provider config",
    "config-set-selectors"  : "R→M: save selector overrides for a provider",
    "config-reset-provider" : "R→M: reset provider to defaults",
    "config-reset-all"      : "R→M: reset all providers to defaults",
    "config-test-selector"  : "R→M: querySelector test in live BrowserView"
  },

  "agent_workflow": {
    "step1": "User clicks ⚡ Orchestrate",
    "step2": "Pre-flight: listOpenViews() → block if 0 panels, warn if selected provider missing",
    "step3": "window.api.orchestrate(opts) → IPC → orchestration-ipc.js",
    "step4": "OrchestrationEngine.run() → filters providers by open view",
    "step5": "Emits routing event → renderer logs it",
    "step6": "ParallelExecutor runs providers (concurrency=3, stagger=700ms)",
    "step7": "Each provider: injectPrompt → sendPrompt → waitForComplete (streaming partial)",
    "step8": "partial-response events → streaming cards in renderer",
    "step9": "provider-status events → status chips in renderer",
    "step10": "RankingEngine.rank() → scored results",
    "step11": "orchestrate-result → renderResults() with score bars, copy, diff, export, chain button",
    "step12": "sessionStore.add() → history panel + history.jsonl file",
    "step13": "[Phase 6] User clicks '⬆ Use as Input' → chainFromResult() → task textarea filled, chain depth++",
    "step14": "[Phase 6] User runs again → recursive refinement with chained input"
  },

  "persistent_files": {
    "userData/state.json"          : "Layout presets + last panes + agent defaults",
    "userData/history.jsonl"       : "Phase 6: cross-session task history (max 200 lines, auto-rotated)",
    "userData/provider-config.json": "Phase 6: user-edited selector overrides per provider"
  },

  "boot_sequence": {
    "1_app_ready"          : "app.whenReady() → sessionStore.init() + providerConfig.init()",
    "2_create_window"      : "createWindow() → registers all IPC handlers",
    "3_load_panes"         : "renderer sends load-panes → BrowserViews created",
    "4_views_loaded"       : "All panels load → views-loaded pushed to renderer",
    "5_boot_selftest"      : "engineRef.current.runBootSelfTest(openIds) → selector coverage in progress log",
    "6_health_monitor"     : "HealthMonitor starts after 5s → periodic health checks"
  },

  "known_issues": [
    "Provider DOM selectors may be stale — run boot self-test after real Electron launch",
    "Claude/ChatGPT ProseMirror selector names change with each deploy — edit via ⚙️ Settings",
    "Grok structure varies by account type (Premium vs free)",
    "openProviderView adds pane at right — relayout shifts existing panes",
    "providerConfig overrides are loaded in main.js but NOT yet wired into BaseProvider — selector changes need Electron restart or manual driver update",
    "Settings panel 'Test' requires an open panel for that provider to work"
  ],

  "next_steps": [
    "Wire providerConfig.getSelector() into BaseProvider: override check before each DOM operation",
    "Plugin architecture: userData/plugins/*.js loaded on boot, can register providers/modes",
    "Auto-recovery: if injectPrompt fails, reload provider page and retry once",
    "Multi-window sync: BroadcastChannel to push results to popup windows",
    "Export: Markdown results auto-copy to clipboard option"
  ],

  "last_safe_recovery_point": "PHASE 6 complete — ai-council6-phase6-FINAL.zip",
  "continuation_prompt": "Nastavi AI Council Phase 6 nastavak. Ucitaj docs/current_state.json. Prioritet: (1) wire providerConfig.getSelector() into BaseProvider.injectPrompt/sendPrompt/extractResponse/isStreaming — replace hardcoded selectors with dynamic lookup, (2) plugin loader userData/plugins/, (3) auto-recovery on injectPrompt failure."
}
